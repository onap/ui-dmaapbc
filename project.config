[access]
	inheritFrom = ui
[project]
	description = DCAE DMaaP Bus Controller GUI based on the ECOMP base framework GUI SDK\n\
"(Set to read-only RT#61295)"
	state = read only
[allowSuperproject "doc"]
	matching = refs/heads/casablanca:refs/heads/casablanca
